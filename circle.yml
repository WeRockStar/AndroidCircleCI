general:
    artifacts:
        - /home/ubuntu/AndroidCircleCI/app/build/outputs/apk/

machine:
    environment:
        ANDROID_HOME: /usr/local/android-sdk-linux

dependencies:
    override:
        - echo y | android update sdk --no-ui --all --filter tools,platform-tools,android-24,extra-google-m2repository,extra-android-supports
        - echo y | android update sdk --no-ui --all --filter build-tools-24.0.1
    cache_directories:
        - /usr/local/android-sdk-linux/tools
        - /usr/local/android-sdk-linux/build-tools/24.0.1
        - /usr/local/android-sdk-linux/android-24
test:
    override:
        - (./gradlew test):
            timeout: 420