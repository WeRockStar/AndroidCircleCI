general:
    artifacts:
        - /home/ubuntu/AndroidCircleCI/app/build/outputs/apk/

machine:
    environment:
        ANDROID_HOME: /usr/local/android-sdk-linux

dependencies:
    override:
        - echo y | android update sdk --no-ui --all --filter tools,platform-tools,build-tools-24.0.1,android-24,extra-google-m2repository,extra-android-supports
    cache_directories:
        - /usr/local/android-sdk-linux/tools
        - /usr/local/android-sdk-linux/build-tools/24.0.1
test:
    override:
        - (./gradlew test):
            timeout: 360